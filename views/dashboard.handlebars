{{#if posts.length}}
<section>
  <h2>Your Posts</h2>
    {{#each posts as |post|}}
      {{> dashboard-postinfo post}}
    {{/each}}

</section>
{{/if}}

<button type="button" onclick="{{{new-post}}}" class="btn">➕ New Post</button>

<script src="/js/add-post.js"></script>